{
  "name": "@ch1c0t/makecli",
  "description": "A CLI app for making CLI apps",
  "version": "0.0.4",
  "bin": "./bin/makecli",
  "scripts": {
    "install": "npm run build",
    "build": "coffee --compile --output lib src",
    "start": "coffee --watch --compile --output lib src",
    "test": "jasmine"
  },
  "devDependencies": {
    "coffeescript": "^2.5.1",
    "jasmine": "^3.8.0"
  },
  "dependencies": {
    "@ch1c0t/io": "^0.0.2"
  }
}
