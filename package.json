{
  "name": "@ch1c0t/makecli",
  "description": "A CLI app for making CLI apps",
  "version": "0.0.0",
  "bin": "./bin/makecli",
  "scripts": {
    "dev": "coffee --watch --compile --output lib src",
    "test": "jasmine"
  },
  "devDependencies": {
    "coffeescript": "^2.5.1",
    "jasmine": "^3.8.0"
  }
}
